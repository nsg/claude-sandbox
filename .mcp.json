{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--headless",
        "--browser=chromium",
        "--no-sandbox",
        "--output-dir=/workspace/.playwright-output"
      ]
    }
  }
}
